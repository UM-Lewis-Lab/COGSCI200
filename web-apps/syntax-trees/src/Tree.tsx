import type { Component } from "solid-js";
import { createSignal } from "solid-js";

const Tree: Component = () => {
  // const [rawForm, setRawForm] = createSignal(
  //   "[S [NP [det The] [N cat]] [VP [V meowed]]]"
  // );
  //
  return <div>Hi!</div>;
};

export default Tree;
